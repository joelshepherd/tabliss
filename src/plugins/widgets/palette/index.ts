import { Config } from '../../types';
import Palette from './Palette';

const config: Config = {
  key: 'widget/palette',
  name: 'Random color palettes',
  description: 'Be inspired with this beautiful random color palettes.',
  dashboardComponent: Palette,
};

export default config;
